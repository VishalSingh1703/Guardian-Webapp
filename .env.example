# Base URL of the Guardian-Server API (owned by the server team).
# Leave BLANK during frontend-only development: the API client (lib/api.ts)
# then runs in STUB mode and simulates all backend responses so the full
# QR -> accident -> SOS flow is demoable without a running server.
NEXT_PUBLIC_API_BASE=
